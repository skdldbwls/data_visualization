var svg = d3.select(".TIME_title")
    .append("svg")
    .attr('width', 1200)
    .attr('height', 100)
    .attr('transform','translate(180,0)')

    svg.append('text')
    .attr('x', 250)
    .attr('y', 80)
    .text('[ Time zone of occurrence by crime type ]')
    .style('font-size', '23px')
    .attr("font-family", "sans-serif")
    .attr("font-weight", "bolder")
    .attr("fill", "black")
